# Server Configuration
NODE_ENV=development
PORT=5000

# Database
# For local MongoDB:
# MONGO_URI=mongodb://localhost:27017/attendance_db

# For MongoDB Atlas (recommended):
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/attendance_db?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters
JWT_EXPIRE=7d

# Frontend URL (for CORS configuration)
# Development:
FRONTEND_URL=http://localhost:3001

# Production (after deploying to Vercel):
# FRONTEND_URL=https://your-app.vercel.app

# API Version
API_VERSION=v1

# INSTRUCTIONS FOR DEPLOYMENT:
# 1. Copy this file to .env
# 2. Update MONGO_URI with your MongoDB Atlas connection string
# 3. Generate a strong JWT_SECRET (minimum 32 random characters)
# 4. After deploying frontend to Vercel, update FRONTEND_URL
# 5. Set NODE_ENV=production for production deployment
# 6. Add all these variables to Render environment variables (do not include comments)
